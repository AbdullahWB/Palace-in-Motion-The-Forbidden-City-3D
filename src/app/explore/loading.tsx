import { RouteLoadingShell } from "@/components/ui/route-loading-shell";

export default function Loading() {
  return (
    <RouteLoadingShell
      eyebrow="3D exploration"
      title="Loading the ceremonial axis..."
      description="Preparing the stylized palace scene, route guide, and AI helper surfaces."
      canvasHeightClassName="h-[34rem]"
    />
  );
}
